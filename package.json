{
  "name": "foodiego-root",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix foodiego run dev",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix foodiego run dev\"",
    "build": "npm --prefix server install && npm --prefix foodiego install && npm --prefix foodiego run build",
    "preview": "npm --prefix foodiego run preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
